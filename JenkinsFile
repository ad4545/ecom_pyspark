pipeline {
    agent {
        label 'docker-agent'
    }
    stages { 
        stage("Pytest"){
            steps{
                sh 'pytest -v'
                echo "Listed....."
            }
        }
    }
}