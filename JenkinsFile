pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                script {
                    docker.build('adarsh7r/ecom-pytest:latest').inside {
                        sh 'pytest -v'
                    }
                }
            }
        }

    }
}