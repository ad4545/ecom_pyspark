pipeline {
    agent {
            docker {
                image 'adarsh7r/ecom-pytest:latest'
        }
    }
    stages { 
        stage("Pytest"){
            steps{
                sh 'pytest -v'
                echo "Listed....."
            }
        }
    }
}