pipeline {
    agent {
        docker {
            image 'adarsh7r/ecom-pytest:latest'
        }
    }
    stages {
        stage("PyTest"){
            steps{
                sh 'pytest -v'
            }
        }
    }
}