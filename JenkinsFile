pipeline {
    agent any

    stages {
        stage("Checkout"){
            steps{
                sh 'ls -la'
                sh 'java -version'
                echo "Listed....."
            }
        }
    }
}