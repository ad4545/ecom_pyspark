pipeline {
    agent {
            docker {
                image 'adarsh7r/ecom-pytest:latest'
                label 'docker-agent' // Optional, if you defined labels
        }
    }
    stages { 
        stage("Pytest"){
            steps{
                sh 'pytest -v'
                echo "Listed....."
            }
        }
    }
}